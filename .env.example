# Fill locally or in a deployment platform. Never commit real secrets.
APP_ENV=local
APP_VERSION=v0.1.0
# Leave blank locally to auto-detect the checked-out Git commit. CI/cloud should set it explicitly.
COMMIT_SHA=
